# Contributing to Poker Trainer

Спасибо за интерес к проекту! Мы приветствуем любой вклад в развитие приложения.

## Как начать

1. **Fork** репозиторий
2. **Clone** ваш fork локально
3. Установите зависимости: `npm install`
4. Настройте PostgreSQL и `.env.local` (см. [DATABASE_SETUP.md](./DATABASE_SETUP.md))
5. Инициализируйте БД: `npm run db:init`
6. Запустите dev-сервер: `npm run dev`

## Процесс разработки

### Создание новой фичи

1. Создайте новую ветку от `main`:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения, следуя [Code Style](#code-style)

3. Проверьте код:
   ```bash
   npm run lint           # ESLint проверка
   npx tsc --noEmit      # TypeScript проверка
   npm run build         # Production сборка
   ```

4. Commit изменения с понятным сообщением:
   ```bash
   git commit -m "feat: добавлена поддержка новых позиций"
   ```

5. Push в ваш fork:
   ```bash
   git push origin feature/your-feature-name
   ```

6. Создайте Pull Request в основной репозиторий

### Исправление багов

1. Создайте ветку:
   ```bash
   git checkout -b fix/bug-description
   ```

2. Исправьте баг и добавьте тесты (если применимо)

3. Commit с префиксом `fix:`:
   ```bash
   git commit -m "fix: исправлена загрузка диапазонов Hero"
   ```

## Code Style

### TypeScript

- **НИКОГДА не используйте `any`** - создавайте типизированные интерфейсы
- Используйте `Record<string, unknown>` для динамических объектов
- Включите strict mode в `tsconfig.json`
- Удаляйте все неиспользуемые импорты и переменные

### Redux

- Используйте типизированные хуки: `useAppDispatch`, `useAppSelector`
- Создавайте три версии actions для разных типов столов (sixMax, eightMax, cash)
- Все типы в `lib/redux/types/`, утилиты в `lib/redux/utils/`
- Reducers в `lib/redux/slices/`

### React компоненты

- Используйте `"use client"` для клиентских компонентов
- Получайте данные из Redux, не через props
- Используйте функциональные компоненты с hooks
- Именование: PascalCase для компонентов, camelCase для функций

### Naming Conventions

- Позиции: `BTN`, `SB`, `BB` (uppercase)
- Сила игрока: `fish`, `amateur`, `regular` (lowercase)
- Стиль игры: `tight`, `balanced`, `aggressor` (lowercase)
- Размер стека: `very-small`, `small`, `medium`, `big` (lowercase с дефисом)
- Действия (UI): `bet-open`, `raise-3bet`, `raise-4bet` (lowercase с дефисом)

### Файловая структура

```
feature/
├── components/          # React компоненты фичи
├── lib/
│   ├── redux/
│   │   ├── types/      # TypeScript типы
│   │   ├── utils/      # Утилиты
│   │   └── slices/     # Redux slices
│   └── utils/          # Общие утилиты
└── app/                # Next.js pages
```

## Commit Messages

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода (не влияет на логику)
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - изменения в build process, зависимостях

**Примеры:**
```
feat: добавлена поддержка 9-max столов
fix: исправлена загрузка Hero диапазонов
docs: обновлена документация API
refactor: оптимизирован tableSlice (1809 → 1483 строк)
```

## Pull Request Guidelines

### Checklist перед созданием PR

- [ ] Код проходит `npm run lint`
- [ ] Код проходит `npx tsc --noEmit`
- [ ] Код проходит `npm run build`
- [ ] Добавлена/обновлена документация (если нужно)
- [ ] Протестирована функциональность вручную
- [ ] Нет использования `any` типа
- [ ] Нет console.log в production коде

### Описание PR

Включите в описание:

1. **Что изменено**: Краткое описание изменений
2. **Зачем**: Причина изменений (баг, новая фича, улучшение)
3. **Как протестировать**: Шаги для проверки изменений
4. **Screenshots**: Если изменения визуальные

**Пример:**
```markdown
## Что изменено
Добавлена поддержка Hero диапазонов без уровня strength

## Зачем
Hero должен иметь упрощённую структуру диапазонов (без fish/amateur/regular)

## Как протестировать
1. Откройте конструктор диапазонов
2. Выберите "Hero" в селекторе
3. Убедитесь, что селектор strength отсутствует
4. Сохраните диапазон
5. Проверьте загрузку в игре

## Связанные issues
Closes #123
```

## Работа с документацией

При изменении кода обновляйте соответствующую документацию:

- **README.md** - общее описание проекта
- **CLAUDE.md** - техническая документация для разработки
- **API_DOCUMENTATION.md** - описание API endpoints
- **CHANGELOG.md** - история изменений

## Отчёты о багах

Используйте Issues на GitHub. Включите:

- **Окружение**: ОС, версия Node.js, браузер
- **Шаги воспроизведения**: Детальные шаги
- **Ожидаемое поведение**: Что должно происходить
- **Фактическое поведение**: Что происходит на самом деле
- **Screenshots/Logs**: Если применимо

## Вопросы?

Если у вас есть вопросы:
- Создайте Issue с меткой `question`
- Изучите [CLAUDE.md](./CLAUDE.md) для технических деталей
- Проверьте существующие Issues и PR

---

Спасибо за вклад в развитие проекта! 🎉
