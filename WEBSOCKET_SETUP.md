# WebSocket Multiplayer Setup

Инструкция по запуску мультиплеер режима с WebSocket.

## Что реализовано

✅ WebSocket сервер для real-time синхронизации между игроками
✅ Создание и присоединение к комнатам
✅ Автоматическая синхронизация состояния игры
✅ Отображение списка доступных комнат
✅ Автоматическое переподключение при потере соединения

## Запуск WebSocket сервера

### Вариант 1: Запуск в отдельном терминале (рекомендуется)

1. Откройте **первый терминал** и запустите Next.js:
```bash
npm run dev
```

2. Откройте **второй терминал** и запустите WebSocket сервер:
```bash
npm run ws:server
```

3. Откройте браузер на http://localhost:3000/tables/multiplayer

### Вариант 2: Запуск в одной команде (Windows)

Создайте файл `start-all.bat`:
```bat
@echo off
start cmd /k "npm run dev"
timeout /t 2
start cmd /k "npm run ws:server"
```

Запустите: `start-all.bat`

## Как использовать

### Создание комнаты

1. Перейдите на `/tables/multiplayer`
2. Нажмите "➕ Создать новую комнату"
3. Введите название комнаты
4. Выберите тип (🏆 Турнир или 💰 Кэш)
5. Нажмите "Создать комнату"

### Присоединение к комнате

1. Откройте **новую вкладку/окно** браузера (или другой браузер)
2. Перейдите на `/tables/multiplayer`
3. В списке комнат найдите нужную
4. Нажмите "Присоединиться"

### Тестирование на одном компьютере

Для тестирования мультиплеера на одном компьютере:

1. Откройте **2-3 вкладки** браузера
2. В первой вкладке создайте комнату
3. В остальных вкладках присоединитесь к комнате
4. Все изменения синхронизируются в реальном времени

### Тестирование в локальной сети

Для игры с друзьями в локальной сети:

1. Узнайте IP-адрес вашего компьютера:
   - Windows: `ipconfig` → IPv4 адрес
   - Mac/Linux: `ifconfig` → inet адрес

2. Обновите WebSocket URL в `lib/hooks/useMultiplayerWebSocket.ts`:
```typescript
const WS_URL = 'ws://192.168.1.100:8080'; // Замените на ваш IP
```

3. Друзья подключаются через ваш IP:
   - Next.js: `http://192.168.1.100:3000/tables/multiplayer`
   - WebSocket автоматически подключится

## Архитектура

### WebSocket сервер (`server/websocket.ts`)

- Порт: `8080`
- Обрабатывает события:
  - `register` - регистрация клиента
  - `create_room` - создание комнаты
  - `join_room` - присоединение к комнате
  - `leave_room` - выход из комнаты
  - `update_room` - обновление состояния комнаты

### Клиентский хук (`lib/hooks/useMultiplayerWebSocket.ts`)

Автоматически:
- Подключается к WebSocket серверу
- Регистрирует пользователя
- Синхронизирует состояние комнат
- Переподключается при обрыве соединения

### Redux (`lib/redux/slices/multiplayerSlice.ts`)

Новые actions для WebSocket:
- `setAvailableRooms` - обновить список комнат
- `setCurrentRoom` - установить текущую комнату
- `clearCurrentRoom` - очистить текущую комнату

## Troubleshooting

### Ошибка "WebSocket connection failed"

**Причина**: WebSocket сервер не запущен

**Решение**: Запустите `npm run ws:server`

### Комнаты не отображаются

**Причина**: Клиент не подключен к серверу

**Решение**:
1. Проверьте индикатор подключения (зелёная точка)
2. Откройте консоль браузера (F12) для логов
3. Перезапустите WebSocket сервер

### Не синхронизируется между вкладками

**Причина**: Разные пользователи или проблема с WebSocket

**Решение**:
1. Проверьте логи WebSocket сервера
2. Убедитесь что обе вкладки показывают "Подключено к серверу"
3. Попробуйте создать новую комнату

## Следующие шаги

Планируемые улучшения:
- [ ] Добавление чата в комнате
- [ ] Kick игрока (хост может удалить игрока)
- [ ] Приватные комнаты с паролем
- [ ] История игр
- [ ] Spectator mode (наблюдатели)

## Для production

Для деплоя на сервер:

1. Используйте отдельный сервер для WebSocket (не порт 8080)
2. Настройте HTTPS + WSS (защищённый WebSocket)
3. Используйте reverse proxy (nginx) для маршрутизации
4. Добавьте аутентификацию через JWT
5. Используйте Redis для хранения состояния комнат (горизонтальное масштабирование)

## Дополнительно

- WebSocket сервер работает независимо от Next.js
- При падении сервера клиенты автоматически переподключаются
- Пустые комнаты автоматически удаляются
- При выходе игрока роль хоста передаётся следующему игроку
